# 本地启动说明（mall 微服务版）

> 本文档记录**本机跑通整套微服务**（mall 微服务商城）的操作步骤与注意事项。

---

## 一、架构与端口

| 服务 | 端口 | 说明 |
|---|---|---|
| mall-auth | 8401 | 认证中心 |
| mall-gateway | 8201 | 网关（统一入口，knife4j 文档聚合） |
| mall-admin | 8080 | 后台管理 |
| mall-search | 8081 | 搜索服务（ES） |
| mall-portal | 8085 | 商城门户（H5） |
| mall-monitor | 8101 | Spring Boot Admin 监控台（macro/123456） |

| 中间件 | 端口 | 账号 |
|---|---|---|
| MySQL 5.7（Docker） | **3307** | root / root，库 `mall` |
| Redis 7 | 6379 | 无密码 |
| RabbitMQ | 5672 / 15672 | vhost `/mall`，用户 mall / mall |
| Elasticsearch 7.17.20 | 9200 / 9300 | 已装 IK 分词插件 |
| MongoDB 4 | 27017 | 库 `mall-port` |
| MinIO | 9000 / 9001 | minioadmin / minioadmin |
| Nacos 2.1.0 | 8848 / 9848 / 9849 | nacos / nacos |

> ⚠️ **为什么 MySQL 用 3307 而不是 3306？**
> 本机 3306 已被原生 MySQL 8.0 占用（密码未知）。Docker MySQL 5.7 映射到 **3307**，
> 相关 JDBC 配置（Nacos 配置 + 各模块本地 `application.yml`）都已改为 `localhost:3307`。

---

## 二、前置要求

- JDK 21（项目编译 target 17）
- Maven 3.9+
- Docker Desktop + WSL2（镜像走 daocloud 镜像源，见下文）
- 内存 ≥ 16GB（建议，当前 15.2GB 偏紧）

---

## 三、启动中间件

```bash
cd E:\Learning\IDEA\InternshipProgram\mall
docker compose -f docker-compose-env-local.yml up -d
```

容器列表：mysql / redis / rabbitmq / elasticsearch / mongo / minio / nacos。

验证就绪：

```bash
docker ps                                    # 7 个容器全部 Up
curl -s http://localhost:9200                # ES
curl -s -o /dev/null -w "%{http_code}" http://localhost:8848/nacos/   # Nacos = 200
```

> **镜像说明**：本机 Docker Hub 直连不通，compose 里除本地已存在的镜像外，
> 均使用 `docker.m.daocloud.io/...` 前缀（daocloud 镜像源）。

---

## 四、初始化数据库

```bash
# 建库
mysql -h127.0.0.1 -P3307 -uroot -proot \
  -e "CREATE DATABASE IF NOT EXISTS mall DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 导入数据（SQL 在 sql/mall.sql，约 3257 行 / 76 张表）
mysql -h127.0.0.1 -P3307 -uroot -proot mall < sql/mall.sql

# 验证
mysql -h127.0.0.1 -P3307 -uroot -proot -e "SELECT id,username FROM mall.ums_admin;"
```

> 默认后台账号：`admin` / `macro123`

---

## 五、导入 Nacos 配置（关键前置）

各服务通过 `spring.config.import: nacos:mall-xxx-dev.yaml` 从 Nacos 拉取配置，
**5 个 dev 配置必须发布到 Nacos**，否则服务启动报 "config not found"。

```bash
publish() {
  curl -s -X POST "http://localhost:8848/nacos/v1/cs/configs" \
    --data-urlencode "dataId=$1" \
    --data-urlencode "group=DEFAULT_GROUP" \
    --data-urlencode "type=yaml" \
    --data-urlencode "content@$2"
}
publish mall-admin-dev.yaml   config/admin/mall-admin-dev.yaml
publish mall-gateway-dev.yaml config/gateway/mall-gateway-dev.yaml
publish mall-portal-dev.yaml  config/portal/mall-portal-dev.yaml
publish mall-search-dev.yaml  config/search/mall-search-dev.yaml
publish mall-demo-dev.yaml    config/demo/mall-demo-dev.yaml
```

> ⚠️ Nacos 容器挂载了数据卷（`./data/nacos/...`），重建容器后配置仍在。
> 若手动清空过数据卷，需重新执行上面的发布。
> 控制台：http://localhost:8848/nacos （nacos / nacos）

---

## 六、构建（跳过 Docker）

根 pom 的 `io.fabric8:docker-maven-plugin` 绑定在 `package` 阶段，本地构建必须跳过：

```bash
mvn install -DskipTests -Ddocker.skip=true
```

> 首次构建会从 aliyun 镜像下载依赖，约 2~15 分钟。

---

## 七、启动 6 个微服务

每个服务单独开一个终端，按依赖顺序逐个启动，确认日志出现
`Started ...Application` 且 `register finished` 再起下一个：

```bash
java -Xms128m -Xmx512m -jar mall-auth/target/mall-auth-1.0-SNAPSHOT.jar
java -Xms128m -Xmx512m -jar mall-gateway/target/mall-gateway-1.0-SNAPSHOT.jar
java -Xms128m -Xmx512m -jar mall-admin/target/mall-admin-1.0-SNAPSHOT.jar
java -Xms128m -Xmx512m -jar mall-search/target/mall-search-1.0-SNAPSHOT.jar
java -Xms128m -Xmx512m -jar mall-portal/target/mall-portal-1.0-SNAPSHOT.jar
java -Xms128m -Xmx512m -jar mall-monitor/target/mall-monitor-1.0-SNAPSHOT.jar
```

也可输出到日志文件后台运行：`java ... > logs/mall-xxx.log 2>&1`

---

## 八、验证（跑通标准）

```bash
# 1. Nacos 服务列表出现 6 个服务
curl -s "http://localhost:8848/nacos/v1/ns/service/list?pageNo=1&pageSize=20"

# 2. 网关 knife4j 聚合文档
curl -s -o /dev/null -w "%{http_code}" http://localhost:8201/doc.html        # 200

# 3. 经网关登录 → 拿 token
curl -s -X POST "http://localhost:8201/mall-admin/admin/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"macro123"}'

# 4. 带 token 调业务接口
curl -s "http://localhost:8201/mall-admin/product/list?pageNum=1&pageSize=5" \
  -H "Authorization: Bearer <token>"                                          # 200 + 商品数据

# 5. 监控台（浏览器，macro/123456）
#    http://localhost:8101

# 6. 直连服务
curl -s "http://localhost:8080/product/list?pageNum=1&pageSize=3" -H "Authorization: Bearer <token>"   # admin
curl -s "http://localhost:8081/esProduct/search?keyword=华为"                                          # search
curl -s "http://localhost:8085/home/content"                                                            # portal

# 7. （可选）把商品导入 ES 再搜索
curl -s -X POST "http://localhost:8201/mall-search/esProduct/importAll" -H "Authorization: Bearer <token>"
```

---

## 九、停止

```bash
# 停止中间件（保留数据卷）
docker compose -f docker-compose-env-local.yml down

# 停止服务：各终端 Ctrl+C，或任务管理器结束 java.exe 进程
```

---

## 十、本机环境适配记录（与官方默认的差异）

| 项 | 官方默认 | 本机实际 | 原因 |
|---|---|---|---|
| MySQL 端口 | 3306 | **3307** | 3306 被原生 MySQL 8 占用；配置已同步改为 3307 |
| ES 版本 | 7.17.3 | **7.17.20** | 7.17.3 在本机触发 cgroup v2 NPE 无法启动 |
| ES IK 插件 | 需自行安装 | 已装（版本 descriptor 改为 7.17.20） | 分词必需；插件来自单体备份 `ik-plugin.zip` |
| Nacos 端口 | 8848 | **8848 + 9848 + 9849** | Spring Cloud Alibaba 客户端走 gRPC，官方 compose 漏映射 |
| Nacos 数据卷 | 无 | `./data/nacos/...` | 防止重建容器丢配置 |
| 镜像源 | docker.io | docker.m.daocloud.io | 本机 Docker Hub 被墙 |
| 数据卷路径 | /mydata/... | ./data/... | 本地相对路径 |

---

## 十一、常见问题

- **启动报 `config not found`**：Nacos 配置没发布，见第五节。
- **启动报 `Public Key Retrieval is not allowed`**：JDBC 连到了 3306 的原生 MySQL 8。检查配置是否仍指向 3306，应改为 3307。
- **ES 起不来 / NPE cgroup**：换 7.17.20+ 镜像，见上表。
- **服务连不上 Nacos**：确认 9848 端口已暴露、nacos 容器正常。
- **内存不足**：15.2GB 机器建议各 JVM 压到 `-Xmx384m`，或停掉 monitor/demo。
